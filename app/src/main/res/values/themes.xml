<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ChatApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ChatApplication" parent="Base.Theme.ChatApplication"/>
    
    <style name="ButtonColor" parent="Base.Theme.ChatApplication">
        <item name="colorPrimary">@color/chatButtonPrimaryColourDark</item>
    </style>

    <style name="TextColour" parent="Base.Theme.ChatApplication">
        <item name="colorPrimary">@color/black</item>
        <item name="android:textSize">16sp</item>
    </style>
</resources>